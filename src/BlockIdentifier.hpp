#ifndef FSC2FSC_BLOCK_IDENTIFIER_HPP
#define FSC2FSC_BLOCK_IDENTIFIER_HPP

namespace s2m {
    namespace BlockIdentifier {
        enum Enum {
            FL_VERSION = 0xc7,
            PITCH = 0xdf,
            NOTE = 0xe0,
        };
    }
}

#endif
